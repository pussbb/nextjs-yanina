import {Metadata} from "next";
import generateDefaultMetadata from "@/app/metadata";
import Page from "./page";

export const metadata: Metadata = generateDefaultMetadata('Calendar for course «Здорова стопа»')

export default Page